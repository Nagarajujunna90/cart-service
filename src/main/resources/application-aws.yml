spring:
  datasource:
    url: jdbc:mysql://host.docker.internal:3306/e-cart?useSSL=false
    password: root
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
    open-in-view: false